# Configuración para API ML local
external:
  prediction:
    host: "http://localhost"
    port: 8000
    endpoint: "/predict"
    api-key: "no-key-required"
    connect-timeout: 5000
    connection-timeout: 5000
    read-timeout: 5000

logging:
  level:
    web: DEBUG