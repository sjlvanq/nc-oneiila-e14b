-- Actualizar predicciones para clientes de alto riesgo (simulación)
UPDATE clients SET 
    LAST_PREDICTION_PROBABILITY = 0.85,
    LAST_PREDICTION_CHURN = 1,
    LAST_PREDICTION_TIMESTAMP = '2026-01-10 20:00:00'
WHERE id IN (5, 8, 13);

UPDATE clients SET 
    LAST_PREDICTION_PROBABILITY = 0.75,
    LAST_PREDICTION_CHURN = 1,
    LAST_PREDICTION_TIMESTAMP = '2026-01-10 20:00:00'
WHERE id IN (2, 15);

UPDATE clients SET 
    LAST_PREDICTION_PROBABILITY = 0.92,
    LAST_PREDICTION_CHURN = 1,
    LAST_PREDICTION_TIMESTAMP = '2026-01-10 20:00:00'
WHERE id = 9;
